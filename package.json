{
  "name": "alldl-backend",
  "version": "2.0.0",
  "private": true,
  "description": "Universal social media video/image downloader API for 2025",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "health": "node -e \"require('http').get('http://localhost:3000/health', (res) => { console.log('Health check:', res.statusCode === 200 ? 'OK' : 'FAIL'); process.exit(res.statusCode === 200 ? 0 : 1) }).on('error', (err) => { console.log('Health check: FAIL -', err.message); process.exit(1) })\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "express-rate-limit": "^7.1.5",
    "axios": "^1.6.2",
    "cheerio": "^1.0.0-rc.12",
    "ytdl-core": "^4.11.5",
    "fs-extra": "^11.2.0",
    "node-cron": "^3.0.3",
    "uuid": "^9.0.1",
    "validator": "^13.11.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}